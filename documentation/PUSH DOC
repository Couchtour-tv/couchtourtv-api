PUSH DOC

sanitize output json
// listChannelsMediaLive.js
// createChannelMediaLive.js


--------------------------------------------------

let getMethods = (obj) => {
  let properties = new Set()
  let currentObj = obj
  do {
    Object.getOwnPropertyNames(currentObj).map(item => properties.add(item))
  } while ((currentObj = Object.getPrototypeOf(currentObj)))
  return [...properties.keys()].filter(item => typeof obj[item] === 'function')
}

--------------------------------------------------


create a mediaLIve Channel in QA using gui
what do you need to have to make it create ?
  inputs
  outputs
  cloudfront

can you create the inputs using api invoke ?
can you create outputs using api invoke
start and stop channel using api invoke



delete media live channel
how do i return the rtmp endpoints for the user to input inot their OBS , and do they need anything else
how do i send the url endpoints to the livestream meta data so the player can show the stream to customers